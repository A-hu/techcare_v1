<h1>Notify comment</h1>

<p>
  Hi <%=@user.first_name %>,
  您有新留言！
  <br>
  針對<%= @comment.commentable.scheduled_date %>行程：
  <br>
  留言種類：<%= @comment.comment_category.name %>
  <br>
  留言內容：<%= @comment.content %>
  <br>
	<%= link_to "觀看留言", schedule_url(@user, :related_id => @comment.commentable.requester_id) if @user.caregiver %>
	<%= link_to "觀看留言", schedule_url(@user, :related_id=> @comment.commentable.caregiver_id) if @user.requester %>
</p>
